# NetBSD-11.99-*

# start w latest NetBSD-11.99.*-amd64-install.img.gz

default install using UFS default sizes

# pkgin install doas gh pkgconf mksh libXft 
firefox geany mc mpv yt-dlp mpg123 liberation-ttf
scrot fastfetch colorls ffmpeg6

# echo permit persist :wheel > /usr/pkg/etc/doas.conf
logout

****
as user
$ chsh /usr/pkg/bin/mksh

$ mkdir ~/Source & cd Source
$ git clone https://github.com/ChefIronBelly/NetBSD.git
$ cp user files and root files from /boot /etc /home to system

$ cd Source /dwm /dmenu /st and sudo make clean install
$ doas mkdir /mnt/usb
$ doas audiocfg default 1

# 250613
